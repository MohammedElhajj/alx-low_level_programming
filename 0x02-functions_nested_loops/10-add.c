#include "main.h"

/**
 * add - adds two integers and returns the result
 * @m: The first integeer number
 * @n: The second integer number
 *
 * Return: The result of the addition
 */
int add(int m, int n)
{
	return (m + n);
}
